# Task 1

Goal: compare OOP data transformation with FP data flow.

Requirement implemented:
- Input names: `["Bat", "Dorj", "Suren"]`
- Return names longer than 4 characters in uppercase

Files:
- `src/main.ts`: OOP and FP implementations

How to run:
```bash
cd lab11/daalgavar1
npm run build
npm start
```

What is shown:
- `StudentList` class with `filterLongNames()` and `toUpperCase()`
- `pipe()` based FP flow with pure functions
- short answers for coupling and state
